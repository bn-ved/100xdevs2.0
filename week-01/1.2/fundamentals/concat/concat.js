// 'a' value can be changed later
let a = 1;
a = 2;

// String concatenation
console.log("Variable: " + a);

// 'b' value can't be changed later
const b = 3;

// String concatenation
console.log("Constant value: " + b);

let firstName = "ved";
let age = "18";
let isMarried = false;

// String concatenation
console.log(
  "This person's name is " + firstName + " and their age is " + age +
    "isMarried: " + isMarried,
);
