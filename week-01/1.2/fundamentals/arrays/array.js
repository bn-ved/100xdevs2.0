// Defining an array
const personArray = ["harkirat", "raman", "kirat"];

// Printing each element from the array
for (let person = 0; person < personArray.length; person++) {
  console.log(personArray[person]);
}
